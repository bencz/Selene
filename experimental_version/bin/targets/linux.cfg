# Linux platform bindings.
#
# Selected automatically by --target=<any linux target>.
#
# posix'io (src/posix/io.sel) provides the console bindings; its classes talk
# to named natives implemented in runtime/natives.c, which is where the
# platform actually lives. See docs/plan/19-runtime-in-c.md.

[forwards]
'program'output=posix'io'stdoutput
'program'input=posix'io'stdinput
