# macOS platform bindings.
#
# Selected automatically by --target=<any macos target>. Empty for the same
# reason as linux.cfg: the POSIX library modules do not exist yet. macOS will
# share most of posix'io with Linux, differing mainly in the FFI library names
# (libSystem rather than libc) -- see docs/plan/18-ffi-design.md section 6.

[forwards]
