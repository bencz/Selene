# Linux platform bindings.
#
# Selected automatically by --target=<any linux target>.
#
# INTENTIONALLY EMPTY FOR NOW. The POSIX library modules that would satisfy
# these names do not exist yet -- 'program'output needs a posix'io module, which
# is part of the runtime rewrite (see docs/plan/19-runtime-in-c.md).
#
# Leaving it empty is deliberate: a program built for Linux reports its platform
# bindings as unresolved, which is true. Pointing them at win32'io would compile
# and then produce something that cannot run.
#
# When posix'io lands:
#   'program'output = posix'io'stdoutput
#   'program'input  = posix'io'stdinput

[forwards]
