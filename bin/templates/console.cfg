[project]
start=$package'elena'3

[linker]
type=1

# Only the SHAPE of the program belongs here. Platform bindings moved to
# targets/<os>.cfg, which --target selects automatically.
[forwards]
'entry=sys'templates'simple
'program'console=ext'io'console
